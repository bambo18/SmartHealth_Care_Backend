서버 실행방법:
1. WSL 실행
sudo service docker start
2.각자 백엔드 폴더로 이동 /mnt....을 참고해서 GPT에게 물어보세요


3.독커실행 서버에 환경변수 설정 및 DB연결
set -a
source .env.local
set +a

docker compose up -d

4. 스프링부트 서버 시작

./gradlew bootRun --args='--spring.profiles.active=dev --server.address=0.0.0.0'


DB확인방법

각자 WSL 접속 후 
docker exec -it postgres_smarthealthdog psql -U postgres -d smart_health_dog

